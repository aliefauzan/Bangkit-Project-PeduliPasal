<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="8dp">

    <!-- Bubble Pesan Terkirim -->
    <TextView
        android:id="@+id/tv_userMessageText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_rectangle_gray"
        android:padding="12dp"
        android:textSize="14sp"
        android:textColor="@color/md_theme_onSurface"
        android:maxWidth="250dp"
        android:gravity="start"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="Ini adalah pesan terkirim yang contoh." />

    <!-- Timestamp -->
    <TextView
        android:id="@+id/tv_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@color/md_theme_onSurfaceVariant"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toStartOf="@id/tv_userMessageText"
        app:layout_constraintBottom_toBottomOf="@id/tv_userMessageText"
        tools:text="09:00" />

</androidx.constraintlayout.widget.ConstraintLayout>
